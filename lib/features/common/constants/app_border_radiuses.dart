import 'package:flutter/material.dart';
import 'package:money_lover_clone/features/common/constants/app_sizes.dart';

class AppBorderRadiuses {
  const AppBorderRadiuses._();

  static BorderRadius small = BorderRadius.circular(AppSizes.x1);
  static BorderRadius medium = BorderRadius.circular(AppSizes.x3);
  static BorderRadius large = BorderRadius.circular(AppSizes.x5);

  static BorderRadius rounded = BorderRadius.circular(100);
}
